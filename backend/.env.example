PORT=4000
DATABASE_URL="file:./dev.db"
NODE_ENV=development
USE_FAKE_STORE_API=false

# Email Configuration (Optional - for production)
# Leave empty for development (emails will be logged to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Vibe Commerce <noreply@vibecommerce.com>"

# Email Configuration (Optional - for receipt emails)
# For development, use Ethereal (fake SMTP) or Gmail
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your-email@ethereal.email
SMTP_PASS=your-password
SMTP_FROM=noreply@vibecommerce.com
ENABLE_EMAIL=false
